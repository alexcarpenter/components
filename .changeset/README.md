# Changesets

Changesets record user-facing package changes before they are released.

Run `pnpm changeset`, select `@alexcarpenter/components`, choose the appropriate
semantic version bump, and describe the change for the changelog. Commit the
generated Markdown file with the pull request that introduces the change.

Changes that do not affect the published package do not need a changeset.
